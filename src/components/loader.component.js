import {Component} from "../core/component";

export class LoaderComponent extends Component {
    constructor(id) {
        super(id);
    }

    show() {
        super.show();
    }

    hide() {
        super.hide();
    }
}